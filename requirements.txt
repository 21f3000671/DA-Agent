# Minimal dependencies for Vercel deployment (under 250MB limit)
fastapi==0.116.1
python-multipart==0.0.20
openai==1.99.3
requests==2.32.4
beautifulsoup4==4.13.4
pandas==2.3.1
python-dotenv==1.1.1
uvicorn==0.35.0
boto3==1.35.0

# All data analysis dependencies are handled with graceful degradation
# Agent will work with basic pandas operations and web scraping
# Heavy packages (numpy, matplotlib, seaborn, sklearn, duckdb, networkx, botocore) 
# will be imported conditionally and fallback gracefully if not available